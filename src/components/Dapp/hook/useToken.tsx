import { BigNumber } from "ethers";

function useToken(
    tokenAddress: string,
    address: string ,
): BigNumber | undefined {
    // const { value: tokenBalance } =
    return
}

export default useToken